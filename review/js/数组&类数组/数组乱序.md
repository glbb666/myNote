```javascript
function shuffle(arr){
	for(let i = arr.length;i>0;i--){
		let j = Math.floor(Math.random()*i);//Math.random()伪随机数,范围[0,1)
		[arr[i-1],arr[j]] = [arr[j],arr[i-1]];//思想,一个数和包括它和之前所有的数做交换
	}
}
```

