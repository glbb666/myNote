### display = none,visibility=hidden,opacity=0的区别

> 渲染树的具体概念在浏览器中

| 特性                         | `display: none`    | `visibility: hidden`           | `opacity: 0`            |
| ---------------------------- | -------------------- | -------------------------------- | ------------------------- |
| **是否在渲染树中**     | ❌ 不在              | ✅ 在                            | ✅ 在                     |
| **占据空间**           | 不占据               | 占据                             | 占据                      |
| **是否触发事件**       | 不触发               | 不触发                           | 触发                      |
| **是否可被子元素覆盖** | 不可（子元素也消失） | 可（子元素设 `visible`可显示） | 不可（视觉上永远叠加）    |
| **是否可过渡动画**     | 不可                 | 不可                             | 可（支持 `transition`） |
| **性能影响**           | 重排+重绘            | 仅重绘                           | 仅重绘（可能合成层）      |
